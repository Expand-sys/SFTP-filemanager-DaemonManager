/// <reference types="node" />
import type { FastifyPluginAsync } from 'fastify';
import type { CookieSerializeOptions } from 'fastify-cookie';
import { SecretKey, SessionCrypto } from './crypto';
import { SessionStore } from './store';
import './typings';
export declare const DEFAULT_COOKIE_NAME = "Session";
export declare const DEFAULT_COOKIE_PATH = "/";
export declare type FastifySessionOptions = {
    salt?: Buffer | string;
    secret?: Buffer | string;
    key?: SecretKey;
    cookieName?: string;
    cookie?: CookieSerializeOptions;
    store?: SessionStore;
    crypto?: SessionCrypto;
    saveUninitialized?: boolean;
    logBindings?: Record<string, unknown>;
};
export declare const plugin: FastifyPluginAsync<FastifySessionOptions>;
